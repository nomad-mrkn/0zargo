apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Only Jobs - jit-rbac-marker resources are excluded
  # jit-rbac-marker resources should be deployed separately as shared infrastructure
  - jobs.yaml